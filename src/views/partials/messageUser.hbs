{{! MENSAJES ADMIN}}
{{#if messageUser}}

  <!-- Alerta -->
  <div class='alert alert-success col-12' role='alert' id='alert'>
    {{messageUser}}
    <button
      type='button'
      class='close'
      data-bs-dismiss='alert'
      aria-label='Close'
    >
      <span aria-hidden='true'>&times;</span>
    </button>
  </div>
{{/if}}

{{#if messageUserFail}}

  <!-- Alerta -->
  <div class='alert alert-danger col-12' role='alert' id='alert'>
    {{messageUserFail}}
    <button
      type='button'
      class='close'
      data-bs-dismiss='alert'
      aria-label='Close'
    >
      <span aria-hidden='true'>&times;</span>
    </button>
  </div>
{{/if}}
