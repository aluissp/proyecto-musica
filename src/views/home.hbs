<div class="container p-4">
  <div class="row">
    <div class="col-12 mx-auto">
      <div class="card text-center border-primary">
        <div class="card-body">
          <h3>
            {{#if user.isArt}}
              Bienvenido
              {{user.seudonimo_art}}
            {{/if}}
            {{#if user.isUsu}}
              Bienvenido
              {{user.nombres}}
            {{/if}}
          </h3>
        </div>
      </div>
    </div>
  </div>
</div>
